{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceFolder}/build/main",
            "cwd": "${workspaceFolder}",
            "presentation": {
                "hidden": false,
                "group": "debug",
                "order": 1
            },
            "preLaunchTask": "mac_build",
            "coreDumpPath": "${workspaceFolder}/build/dump"
        }
    ]
}